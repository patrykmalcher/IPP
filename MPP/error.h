// Moduł mający na celu wyświetlanie komunikatu o błędach.
#ifndef ERROR_H
#define ERROR_H

#include <stdio.h>

// Wypisuje ERROR o podanym numerze na standardowe 
// wyjście błędów (stderr).
void printError(size_t digit);

#endif